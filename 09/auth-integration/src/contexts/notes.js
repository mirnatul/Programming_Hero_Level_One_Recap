/**
 * 1. create a context. so that we can share the state of user across the app
 * 2. create a provider to re-use the context
 * 3. set create user via context
 * 4. to login or another tab user will lost (firebase-manage user - get currently signed in user)
 */