/**
 * 1. e.target.name.value
 * 2. use form action and formdata using get method
 * 3. controlled. (one per each field) onchange (another function to monitor) and default value
 * 4. handle all controlled field on one state object
 * 5. uncontrolled using useRef
 */